import { DetailsHostDirective } from './details-host.directive';

describe('DetailsHostDirective', () => {
  it('should create an instance', () => {
    const directive = new DetailsHostDirective();
    expect(directive).toBeTruthy();
  });
});
